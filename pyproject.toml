[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "exat"
version = "0.1.0"
description = "Unified attack/defense/explainer abstractions for adversarial evaluation"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
